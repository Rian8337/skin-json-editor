/**
 * Represents the `ComboColor` section of skin.json.
 */
export interface SkinJsonTheme {
    /**
     * The accent color of the theme, in hex code.
     * 
     * Defaults to `#C2CAFF`.
     */
    accentColor?: string;
}